# Tough Trial 交付路线

更新日期：2026-09-20

## 今日评审落地与真实页面基线（2026-09-20）

- 已实现：主卡片与执行队列互斥、暂停/完成后队首补位、今日规划保留完成态、本段/今日/跨天累计用时。
- 已验证：3 项状态/计时单元测试，开始/暂停/继续/完成/恢复及切换主卡片的真实模拟器点击；核心、兼容检查和 Swift 构建通过。
- 评审素材更新为当前原生模拟器截图（测试数据）；其余仍为设计稿的页面明确标注，不表示已落地。
- 待用户继续评审任务结构及其他模块；本轮未安装手机，也未标记用户手机任务完成。
- 详见 [本轮 QA](qa/2026-09-20-today-queue-native-review.md)。

## 新增计划：可自定义底部导航与「更多插件」（2026-09-20）

用户已确认：`我的资料与快速分享` 作为独立模块，不并入现有五个核心模块。底部导航改为可自定义的常用入口层，完整设计见[可自定义底部导航与「更多插件」](superpowers/specs/2026-09-20-customizable-tabbar-and-more-plugins-design-zh.md)。

| 阶段 | 交付 | 当前状态 | 完成标准 |
| --- | --- | --- | --- |
| N1 · 导航数据层 | 独立 `V2NavigationPreferences`、2–5 项约束、固定 `morePlugins`、旧布局迁移 | 首版已实现 | 独立导航偏好、归一化与持久化已完成；旧版 `hiddenSurfaces` 首次升级迁移仍需补齐 |
| N2 · 动态底栏 | Root 按用户顺序渲染 1–4 个普通模块 +「更多插件」 | 已实现，待交互验收 | iOS Root 已按偏好动态渲染，并处理当前页被移除后的回退；需在真机/模拟器覆盖 2/3/4/5 Tab |
| N3 · 更多插件 | 未放到底栏但已启用的模块启动器；保留现有“功能与插件设置”入口 | 已实现，待交互验收 | 隐藏但启用的模块可直接打开且不会自动加入底栏；停用联动需做交互回归 |
| N4 · 编辑与拖拽 | 编辑页增删、长按拖拽左右顺序、最多 5 个反馈 | 已实现，待交互验收 | 已支持编辑页增删/排序与底栏拖拽；需真机验证拖拽手感、持久化和边界反馈 |
| N5 · 我的资料接入 | `我的资料` 注册为独立可选模块 | 已实现，待交互验收 | 可作为独立模块加入/移出底栏，也可从「更多插件」打开；iOS/macOS 均已编译通过 |
| N6 · 双端与验收 | iPhone 真实交互矩阵；Mac 映射同一常用入口排序 | 后续 | VoiceOver/大字体可操作；布局同步与业务数据解耦 |

实施顺序：**N1 → N2 → N3 → N4 → N5**。模块启用/停用继续由现有插件 runtime 负责；“从底栏移除”只改变导航布局，不停用功能、不删除数据。新模块默认进入「更多插件」，不会自动抢占用户底栏位置。

## 新增计划：我的资料、快速分享与联系人辅助工具（2026-09-20）

产品重心调整为“展示我自己并快速把信息给出去”。设计见[我的资料、快速分享与联系人辅助工具](superpowers/specs/2026-09-20-business-card-contacts-design-zh.md)。一级入口先显示当前身份、`我的二维码`、`分享信息`和社交账号；联系人库与扫名片降为“其他工具”。

| 阶段 | 交付 | 当前状态 | 完成标准 |
| --- | --- | --- | --- |
| C3 我的资料与快速分享（首优先） | 我的资料首页、当前身份、微信等系统分享、名片图片、vCard 二维码、抖音/小红书/GitHub/X 社交账号与二维码、分享范围预设 | 首版已实现，继续完善 | 已完成资料首页、系统分享、真实二维码、社交账号入口与资料编辑；vCard 标准载荷、二维码/名片图片保存分享、分享范围预设、社交二维码图片导入和多身份仍待开发 |
| C1 扫描与建立（次优先） | 拍照/选图、OCR、姓名/公司/职位/电话/邮箱等草稿、原图保留、确认保存 | 待开发 | 识别失败可手填；取消不落正式数据；保存后重启仍在 |
| C2 联系人库（次优先） | 人物 / 公司 / 最近、全文搜索、详情编辑、可选标签 | 待开发 | 搜索主要字段；编辑/撤销可靠；不要求录入前先分类 |
| C4 整理 | 重复提示、逐字段合并、AI 标签和备注候选 | 待开发 | 不静默合并；来源全部保留；AI 结果先确认 |
| C5 关联 | 联系人创建任务、助手只读检索、随手记建议建立联系人 | 待开发 | 引用稳定；普通读取低摩擦；写入仍走领域命令 |
| C6 跨端 | Mac 同功能、联系人和名片进入统一备份/同步协议 | 后续 | 双端冲突可恢复；附件不丢；同步范围展示准确 |

首期先完成 C3：自己的资料卡、面对面二维码、微信/信息分享与社交二维码；之后再推进 C1、C2。分享范围作为辅助设置，不成为日常分享的前置步骤。联系人自动联网补全、公开网页名片和云端同步不并入首期。

## Mac 与 Ontology（2026-09-16）

用户已批准[设计](superpowers/specs/2026-09-16-macos-sync-ontology-design-zh.md)。本轮按[完整客户端计划](superpowers/plans/2026-09-16-macos-full-app.md)交付 **Mac 全功能本地客户端开发版**，仍不等于全量云同步或全部外部服务验收完成。

| 阶段 | 当前进度 | 尚未完成 |
| --- | --- | --- |
| A · Mac 客户端 | 今天执行/计时、任务四视图与连续编辑、分类/归属/安排、随手记/附件、理账/预算/统计、助手、回想、设置；共用同一 engine，保留原资料目录 | 自然中文 IME、语音权限与识别、真实 AI 对话/浏览、通知、长时间稳定性及全交互矩阵待专项验收 |
| A · 统计 | 收入/支出/净额、自然日/周/月/年、确认分类与每日明细；币种分离，转账排除，未知日期明确提示；5 项领域测试通过，Mac 实际切换验证 | 标签模型、点击分类钻取、分类层级汇总、趋势图与手机真机体验 |
| A · Ontology | 44 个节点、38 条关系；覆盖任务编辑、Mac 接线、退出、附件、统计等部分代码与测试；7 项工具测试通过 | 全模块语义调用索引、CI 与运行 trace、根因排序未接入 |
| B · 手机/Mac 同步 | Mac 接入现有 GitHub 任务/日程配置与同步界面 | 真实双端远端联调、新对象版本协议、全域冲突/恢复待实施 |
| C · 其他内容 | Mac 笔记、理账、助手、附件等页面已接入本地存储 | 上述内容与媒体全量跨端同步仍待实施 |
| D · AI 故障诊断 | 可查询业务入口、绑定函数与当前源码范围，拒绝陈旧位置 | 跨端事件关联、运行证据与诊断闭环未实现 |

验收见 [Mac 完整客户端](qa/2026-09-16-macos-full-app.md)。24 项任务/统计测试、8 项 Mac 集成测试通过；Mac 与 iOS Simulator 编译通过。全部写入验收使用隔离的合成资料，未上传个人内容。旧版验收保留在 [Mac 首版与 Ontology](qa/2026-09-16-macos-ontology.md)。

真机进展：9 月 14 日 A–D 体验修复版 1.0（11）已完成正常安装启动；四视图修订版 **1.0（13）已安装并正常启动，39 条任务数据核对一致**。6 项文档输入组件设备测试及手机生产搜索探测通过；真实 MiniMax 联网会话未通过（未稳定调用网页工具，未得到来源），D 继续开放。自然语音、真实输入法与原用户回访仍待验收；当时财务统计页尚未开发（9 月 16 日 Mac/共享统计新增进展见上方）。见[本轮真机验收](qa/2026-09-14-device-feedback-verification.md)。

## 四个并排任务视图（2026-09-14 用户修订）

- [x] 顶部一行“列表、结构、时间、鱼骨”，默认列表，取消结构页内的第二层切换。
- [x] 列表平铺全部任务（含子任务），标题直接打开详情；结构直接打开树状图。
- [x] 保留原任务关系、共用编辑器、时间安排及鱼骨行为。
- [x] 8 项模拟器交互测试通过，领域/兼容检查、Swift 包及 iOS 设备构建通过。
- [x] 1.0（13）原位安装并正常启动；安装前后 39 条任务及完成状态一致。
- [ ] 用户在手机测试后自行标记反馈任务完成。

验收与截图见[四视图验收](qa/2026-09-14-four-task-views.md)。下方 1.0（12）是历史实现记录，其二级切换已由本轮四视图替代。

## 真机反馈续修：直接编辑与树状图（2026-09-14）

- [x] 详情点击标题、正文或空正文提示进入共用文档编辑器，移除独立编辑按钮并自动请求键盘。
- [x] 结构页始终提供“列表 / 树状图”切换，全部根任务可进入总览；不自动建立父子关系。
- [x] 8 项 iPhone 17e 模拟器交互测试通过：无层级/空态、标题和正文编辑、保存/取消/撤销、分类、长内容及大字体；核心、兼容检查和包构建通过。
- [x] 正常版 1.0（12）已原位安装到 iPhone 13 Pro，安装前后 39 条任务及完整快照一致。
- [x] 正常启动：首次被锁屏阻止，22:18 重试成功，启动后再次核对 39 条任务一致。
- [ ] 原用户手感回访、自然语音与真实输入法复验；模拟器通过不等于这些体验完成。

详见[本批验收](qa/2026-09-14-direct-edit-and-tree.md)及[树状图截图](qa/assets/2026-09-14-direct-edit/flat-task-tree.png)。本项不代表财务统计、真实模型联网对话或全量备份完成。

## 已确认方向：本地优先与多渠道备份（2026-09-14）

用户确认：**本地保存、多渠道备份，当前只开发 GitHub；预留 WebDAV/NAS。** 后续将端到端加密、跨设备冲突处理、版本恢复和选择性同步作为托管收费服务的候选能力。详见[备份与同步设计](superpowers/specs/2026-09-14-local-backup-and-sync-design-zh.md)与[Obsidian 官方机制调研](sync/2026-09-14-obsidian-sync-research.md)。

本轮完成的是方向确认、现状审计和调研。**现有 GitHub 只覆盖日程同步；新增全量备份与恢复尚未开发。** 不把现有日程版本历史当作财务、笔记、附件和助手资料已备份。

| 阶段 | 交付 | 当前状态 | 完成标准 |
| --- | --- | --- | --- |
| 决策与调研 | 本地优先、GitHub 首期、WebDAV/NAS 扩展；Obsidian 五项机制与收费边界 | 已完成本轮文档 | 明确已有能力、首期范围和后期候选 |
| G1 本地备份 | 统一版本包、领域与附件清单、校验、恢复保护副本 | 待开发 | 完整往返；损坏/缺附件/恢复中断保全本地；不重放副作用 |
| G2 GitHub 渠道 | 私有数据仓库、不可变版本、上传/列表/下载与可重试发布 | 待开发 | 合成数据真实远端恢复；并发、断网、限流不丢数据 |
| G3 App 入口 | 应用主题的备份与恢复页；连接、立即备份、状态、恢复预览 | 待开发 | 实际点按验证主操作、取消、失败、重试和禁用路径 |
| G4 真机闭环 | 手机备份、重启、另一设备下载恢复 | 待验收 | 数据和附件一致，无凭据外泄；真实数据范围另行明确 |
| WebDAV / NAS | 同一备份格式与渠道接口；NAS 优先复用 WebDAV | 后续预留 | 首期不实现，不显示假连接入口 |
| 托管 Sync 收费探索 | E2EE、业务冲突处理、设备范围、长期历史、大容量与可靠性服务 | 官方调研完成，产品原型未开发 | 密钥恢复/撤销、安全审查、跨端与成本验证后才发布/定价 |

- [x] 固化用户确认的方向，区分备份与双向同步，审计现有日程实现范围。
- [x] 查证 Obsidian E2EE、冲突、版本历史、选择性同步、运行和容量限制，记录官方来源。
- [ ] 完成 G1–G4，只实现 GitHub 渠道；本地保存不依赖网络、账号或付费状态。
- [ ] GitHub 首次连接展示数据范围；开发验收只用合成数据，不把方向确认当作上传全部个人资料的授权。
- [ ] 后续收费验证：本地与自有存储保留；收费聚焦托管省心、空间、长期历史和恢复服务，基础恢复不设付费门槛。
- [ ] E2EE 与云端 AI 边界验证：加密密钥不交给 GitHub Actions；云端 AI 仅处理用户该次主动授权的内容。

多渠道是备份副本的扩展，不是同时开启多套双向写入。Android/Web 客户端另行开发；iOS 后台运行不承诺永久在线。现有日程同步和此前未完成的财务统计、真机体验验收继续保留，不因本项而标记完成。

## 新增计划：财务统计页（2026-09-13）

用户提出按分类、标签及自然时间查看统计。**9 月 16 日已实现共享统计基础页并接入 Mac 与记账入口**；标签、分类钻取与手机真机验收仍未完成，不计入此前 C 批次的历史完成状态。

建议从现有记账页进入「统计」，复用同一份正式流水，沿用应用主题；默认打开本月结果，无需先填写筛选条件。

| 维度 | 拟定体验 | 统计口径 / 待确认事项 |
| --- | --- | --- |
| 收支概览 | 当前期间收入、支出、收支差额；可直接查看每日进账 | 差额 = 收入 − 支出；转账独立展示，不计入收入或支出；不同币种分别统计 |
| 自然时间 | 日 / 周 / 月 / 年，可前后切换周期；默认本月每日趋势 | 按真实发生日期，非滚动 7 / 30 天；拟定周一至周日、自然月和自然年；日期缺失单独列出，不用录入时间冒充发生日期 |
| 分类 | 收入和支出分别展示分类金额、笔数，点分类看对应流水 | 使用已确认分类；未确认归 Others；父子分类汇总时每笔只计一次 |
| 标签 | 按标签查看金额、笔数及对应流水 | 当前没有独立标签字段；已询问用户，尚待确定采用「独立多标签」还是「分类的另一种叫法」。若采用多标签，一笔可出现在多个标签下，总收支按流水 ID 去重，标签金额不可直接相加 |

- [ ] 确认标签含义并完成页面及数据设计；可选标签不增加记账必填项，不打断连续输入。
- [x] 实现可复用统计基础逻辑与页面，入口接入现有记账页及 Mac 理账；统计仅读取正式流水。
- [ ] 覆盖自然日/周/月/年边界、跨年与闰日、无发生日期、多币种、转账、分类层级及标签重复计数。
- [ ] 实际验证默认打开、周期切换、分类/标签明细、返回、空态与大字体；真实手机体验单独验收。

上述未勾选项保留为后续范围；此前 SVG 状态表为 9 月 12 日快照，未包含本项。

## 下一批优先：用户反馈体验修复（2026-09-11）

来自照片 1–6 及“任务点开后无法编辑”。**A 批次已实现并通过核心、App 集成与两种尺寸模拟器交互验收；真机及原用户回访尚未完成。B 已完成实现和定向模拟器验证，自然听写与真机输入法待验收；C 已完成实现与本地回归，真实 AI、自然语音及真机体验待验收，D 已完成本地修复与生产搜索客户端复验，真实模型联网对话待验收。** 详细范围、现状证据和验收步骤见[用户反馈优化计划](superpowers/plans/2026-09-11-user-feedback-ux-roadmap.md)。先修任务基础操作，再统一输入体验；已有助手主页与同步剩余验收继续保留。

| 批次 | 优先级 / 反馈 | 优化交付 | 关闭条件 |
| --- | --- | --- | --- |
| A | P0 · UX-01/02/08 | 任务详情直接编辑标题/备注、保存/取消；完成/恢复；长详情拉高、滚动、路径去重与清晰导航 | 三种任务视图和今天数据一致；重启保留，取消/撤销有效；真实点按验证，不依赖 AI |
| B | P1 · UX-03/06 | 复验长输入并优化同页展开；任务/今天统一新增和显式语音入口 | 长听写光标可见、可滚动、切页不丢稿；任务默认不排期，今天新增仍进入当天 |
| C | P1 · UX-02/04/07 | 任务详情分类入口；现有记账页更易发现；语音→一键整理→字段修改/分类确认 | 分类不侵入今天执行；原文保留、不重复记账、缺字段可补、分类经人工确认 |
| D | P1 · UX-05 | 按真实工具配置复验搜索、联网失败与能力说明 | 成功有来源，失败原因准确且有可操作下一步，不误建任务 |
| E | 发布前 | 原反馈场景真机验收与用户回访 | 每项有构建/操作/状态证据；未回访或未实测的项保持开放 |

- [x] A：任务编辑、完成与长详情基础闭环；核心/App/模拟器通过，真机与用户回访保留在 E。见[本批验收](qa/2026-09-11-user-feedback-ux.md)。
- [x] B 实现：统一任务新增/编辑、显式听写、助手同页长输入；见[本批截图与验收](qa/2026-09-11-unified-input-b.md)。
- [x] 9/12 交互修正：任务采用首段标题、回车后正文的连续文档；正文可空，移除输入/听写目标切换，版式沿用应用主题；提案草稿由稳定页面持有，避免消息重建丢失输入。见[新版截图与验收](qa/2026-09-12-task-document-input.md)。
- [ ] B 体验关闭：60 秒自然听写、真实中文输入法首/中/尾编辑及真机回访；与助手 P0/P5 共用证据。
- [x] C 实现与本地回归：可选任务类型；记账原文→整理→核对保存、人工分类与撤销；复用现有输入组件和领域能力。见[执行计划](superpowers/plans/2026-09-12-classification-ledger-ux.md)及[截图与验收](qa/2026-09-12-classification-ledger.md)。
- [ ] C 体验关闭：真实模型理解口述改口、自然听写及手机操作，随 E 验收。
- [x] D 本地交付：能力开关传给模型，停用/超时等失败给出中文原因与重试；生产搜索客户端实测返回来源，App 与界面定向回归通过。见[联网验收与截图](qa/2026-09-12-web-feedback.md)。
- [ ] D 真实对话关闭：9/14 手机已有可用 MiniMax 配置，生产搜索探测通过，但模型联网会话未通过；继续定位工具调度并复验基于来源作答。
- [ ] E：真机验收、修复版回访与逐项关闭。

### 共同实施方式：项目内部组件组

用户已明确以本项目内部协作开发为主要用途。A 批次先形成共用任务编辑器并接入新增/已有任务编辑，B 批次完成其他任务入口迁移及长文本/语音复用，C 批次让记账复用基础输入组件。业务提交继续遵守各自领域规则，助手未确认提案只改草稿。组件在现有 App target 内按基础交互与业务分组，附带输入/事件说明、示例预览、测试和入口迁移清单；详见[组件组与迁移边界](superpowers/plans/2026-09-11-user-feedback-ux-roadmap.md#项目内部组件组与迁移边界)。

- [x] A：同一任务编辑器覆盖任务页结构/鱼骨新增与已有任务编辑，保存/取消和重启验证通过；已提供组件 README 与预览。
- [x] B：今天、任务、快速待办、助手提案已接入共用任务编辑器；Input 组件组提供听写和多行输入，草稿彼此隔离。
- [x] C：记账复用输入组件且保留分类确认；组件说明、预览及定向交互回归已补齐。
- [ ] E：组件对应的自然语音、真实中文输入法和真机回访。

此项随体验修复渐进实施，不单开全库重构阶段，不要求打包成对外 SDK。

A/B/C/D 已完成本地实现与相应回归，后续推进真实模型对话与 E 真实体验验收；自然听写、真机和用户回访继续开放。此处 P0/P1 是本批问题优先级，下方 P0–P5 是原助手项目阶段，编号含义不同。本批不新增独立账本、强制分类流程或自动网页操作。

## 进行中路线：助手主页重做（2026-09-11）

用户已确认方向：助手成为默认主页，纳入与其他页面一致的底部 Tab Bar；重做前端及对话调度，支持 Coding Plan / 模型 / Thinking Effort 切换。P0–P4 已落地，核心与 App 回归及 8 项 UI 场景通过；1.0 (5) 已签名、安装到 iPhone 13 Pro 并成功启动。P5 尚余真实 Coding Plan 自然多轮对话和真机完整交互验收。详见 [本次验收记录](qa/2026-09-11-assistant-home.md)。下方原路线及历史验收继续保留。

### 产品形态

- 默认进入“助手”；建议 Tab 顺序为“助手 / 今天 / 任务 / 随手记 / 回想”，沿用现有模块显隐与停用规则。助手关闭时选取可用页面。
- 助手直接嵌入主 Tab，不再先进入入口页再打开全屏助手。顶部放会话切换与新对话，主体是连续消息，底部是输入区；沿用项目字体、颜色、间距与系统导航。
- 从任务、笔记等入口发起提问时切换至助手 Tab，保留可见来源和返回原对象入口。切 Tab 不丢草稿、引用、滚动位置或正在进行的会话。
- 输入区提供展开编辑、附件、语音、发送；附近显示当前服务、模型与思考设置。日常控件有独立入口，高级诊断进入上下文面板。
- 通用对话、分析、检索、笔记、理账和日程都属于助手能力。普通回答可自由表达，业务工具仍按既有 schema 和事务执行。

### 分阶段交付

| 阶段 | 用户能用到什么 | 完成标准 |
| --- | --- | --- |
| P0：长输入与消息操作 | 输入自动增高、内部滚动、全屏编辑；双方消息选择/复制/引用；草稿保存 | 长段中文听写、粘贴、多行编辑、大字体下光标可见；切页返回不丢字；引用发送后准确到达模型 |
| P1：助手主页与统一前端 | 启动进入助手、底部 Tab 导航、统一消息/工具/结果视觉；空白/持续聊天/运行/错误状态完整 | 无二次打开全屏页；切 Tab 后会话状态保留；任务来源可返回；键盘与输入区不互相遮挡 |
| P2：AI 服务与模型控制 | 服务/Coding Plan → Model → Thinking Effort 三层选择；会话设置与默认设置分开 | 已配置服务可切换；只显示实际支持的思考选项；下一轮所有模型请求使用一致设置，Trace 可核验 |
| P3：灵活对话与连续执行 | 自由聊天、多工具协作、运行中补充；语义理解替代关键词白名单；执行后继续解释与跟进 | 口语请求可执行，问候不继承旧授权；混合“分析→检索→记笔记/任务”链路完成；停止/重试不重复写入 |
| P4：可见且有效的 Compact | 展示压缩前后规模、覆盖消息、保留要点、当前引用；原文回查；无需压缩时如实提示 | 下一轮实际发送量确实下降，摘要无收益时不替换；引用与待办约束保留；前后指标区分实测 token 与字符/估算 |
| P5：真实体验验收与发布 | 连贯的手机体验和可追踪问题记录 | 真机完成自然多轮对话、混合功能、跨 Tab、语音长输入、消息引用、模型切换、压缩、撤销；安装/启动各有回执 |

建议实施顺序：P0 → P1 → P2 → P3 → P4 → P5。P0 可独立交付；P3 的输入引用与补充需使用 P0/P1 的消息结构，P4 的摘要请求沿用 P2 的模型能力配置。

### P0：输入与引用

- [x] 原生多行编辑器：按内容增高，达到高度上限后内部滚动，支持全屏展开，中文输入法组合态不丢失。
- [x] 草稿按会话保存；语音转写和手动修改共享同一草稿，录完由用户提交。
- [x] 双方消息支持复制全部、选择文字、引用；引用预览可移除、可回到原文。
- [x] 引用以 message ID、来源和选中文字快照传递并持久化；不能只画一个引用卡片而不进入模型上下文。
- [ ] 验收：长文本/大字体/键盘、引用跨重启、原消息修改或删除后的来源状态。

### P1：主页与视觉

- [x] 移除入口页到全屏助手的二次导航，助手作为默认 Tab 页面；兼容插件隐藏、停用和快捷入口。
- [x] 重做空白会话、长消息、持续对话、工具运行、结果、错误/重试状态；正文优先，工具详情可折叠。
- [x] 顶部会话与新对话，底部输入与模型选择，上下文提供轻量入口；沿用现有设计语言。
- [ ] 验收：Tab 往返、键盘避让、当前引用可见、正在运行的会话不会被误取消或换到其他会话。

### P2：服务 / 模型 / 思考强度

- [x] 复用当前供应商独立配置及 Keychain；优先迁移已配置服务，不自动切换账户、不重新收集已有密钥。
- [x] 已核验并实现 thinking 参数与可用强度映射；未核验的模型只显示自动。
- [ ] 补全工具、流式与上下文上限的统一能力目录；本次未新增流式输出。
- [x] UI 可展示“自动 / 关闭 / 低 / 中 / 高”等，但仅按模型真实能力显示；仅支持开关则显示开关，不支持则说明；不凭空映射通用档位。
- [x] 会话保存选择，另设“用于新对话”；进行中的回合固定原配置，切换从下一轮生效并显示提示。
- [x] 聊天、任务整理、规划和动态模型工具使用同一回合固定配置；凭据不进入会话日志。Compact 当前采用确定性的有界摘要，不发起额外模型请求。
- [x] 启用 Trace 时记录实际服务、模型和思考设置；参数错误明确返回。
- [ ] 供应商实际 token/时延与多轮参数兼容性仍需真实调用验收。

### P3：通用助手

- [x] 普通回答不强制变成日程；读取现有可用插件工具目录，支持笔记、理账、复盘和任务等已实现能力。
- [x] 对当前用户意图做语义判断，并绑定当前回合；历史消息、工具结果与引用材料不能单独赋予写入授权。
- [x] 宿主校验 schema、目标 ID、模块、数据证据和事务，保留高亮、撤销与严格确认选项；账单分类及支付确认仍遵循既有规则。
- [x] 模型根据工具回执继续回复；缺信息时提出针对性问题，不要求用户背固定句式。
- [x] 工作中允许编辑下一条输入；发送补充排队，在当前回合结束后作为下一回合处理，展示已接收，支持取回/取消；不改变已完成操作的授权。
- [ ] 验收语料覆盖口语、否定、改口、引用、只讨论、跨功能请求及连续追问；真实模型使用独立合成数据验证实际业务结果。

### P4：上下文整理

- [x] 先修正空压缩/重复压缩的成功提示；无需整理或不能减少上下文时明确说明。
- [x] 摘要保留目标、决定、约束、待办、未决问题和来源；摘要生成失败不替换原上下文，不自动写入长期记忆。
- [x] 展示本次整理覆盖多少消息、整理前后上下文规模和摘要；指标基于有效请求，不能只比较存档文件大小。
- [ ] 在下一次请求 Trace 中验证摘要版本和来源仍在，原文可回查；归档与当前模型上下文清楚分开。

### P5：验收与现有遗留

- [x] 1.0 (5) 已包含口语提案修复，完成签名、真机安装与启动。
- [ ] 真实 GLM 口语多轮效果仍待验收，不以本地合成客户端代替。
- [ ] 首先评审主要页面状态，再做模拟器键盘/长消息/引用测试，最后用真机和真实服务验收实际输入到结果。
- [ ] 多轮验收包含：随意聊天 → 引用旧消息 → 查笔记 → 提炼想法 → 生成任务 → 改口 → 撤销；不能用固定单句测试代替。
- [x] 报告区分本地测试、真实模型结果、真机 UI 和实际安装；保留失败样本及 Trace。

### 复用边界与实现入口

保留 Core schema、Engine 事务、插件目录、已有记忆与日志、同步协议；本路线重做助手交互与调度，不推翻业务数据层。Coding Plan 是服务配置，不是模型，也不等同于一定支持所有 thinking 档位。

主要入口：`V2RootView.swift`、`V2AssistantView.swift`、`V2AssistantSessionViews.swift`、`V2AssistantStore.swift`、`V2AssistantTurnLoop.swift`、`V2AssistantContextStore.swift`、`V2AssistantContextView.swift`、`V2AIProviderSettings.swift` / `V2AIProviderSettingsView.swift`；Core 对应 `V2AssistantWriteIntent.swift`、`V2AssistantContext.swift`、`V2AssistantArchive.swift`、`V2OpenAIRequestCompatibility.swift` 及各模型客户端。实施前分为主页交互、模型配置、调度/Compact 三个可独立验收的详细计划。

---

## 原主线：AI 驱动个人日程（路线 1–5）

语音输入已完成苹果 / FunASR 切换和真机对比。接下来按用户批准的顺序推进：

| 顺序 | 交付 | 验收重点 |
| --- | --- | --- |
| 1 | AI 直接执行日程 | 新增、修改、延期、完成，高亮、整次撤销，可选修改前确认 |
| 2 | 可执行的任务整理 | 保留细节、否定、改口，拆解步骤，继续修改已有任务 |
| 3 | 使用 Trace | 本机有界记录，查看、导出、关闭，定位操作摩擦 |
| 4 | Markdown 执行文件 | 稳定 ID，手工编辑往返，坏文件保护、版本恢复 |
| 5 | GitHub / 云端同步 | 多设备三方合并、离线重试、远端 AI 回写、本地 AI 冲突处理 |

当前状态：执行事务与 Trace 已完成本地流程验证；Markdown 协议、语义合并和云端运行器已集成并通过核心检查。App 文件绑定、读写往返与恢复已通过核心/模拟器测试；GitHub 配置、前台自动同步、离线重试与本机 AI 冲突处理已有实现并通过本地检查。运行器已部署到用户指定的私有 `tough-trial-sync` 仓库；GLM-5.3-Flash Coding Plan 真模型日程样本、Actions 自动回写、两个独立持久客户端的真实 GitHub 合并/冲突 AI/恢复/重试均已通过。已修复本轮暴露的限制占位描述、模型分钟换算与相对日期问题。iPhone 文件授权、当前模型配置与真机端到端同步仍待设备恢复连接后验收。以上五项尚未完成整体验收。
用户后续指定文字服务改用 GLM Coding Plan，已按 pi 的端点/鉴权/思考参数接入并完成真实云端回写，详见 [GLM 接入记录](sync/glm-coding-pi.md)。
最新[逐项验收核对](qa/2026-09-07-route-1-5-audit.md)已完成：本机文件/Trace/同步配置复测通过，当前 GLM 云端重复触发不写入新版本，45 个源文件与远端一致。用户暂时没有真机，目标等待实际手机的文件授权、模型配置与端到端同步验收后收尾。
详见[产品规格](superpowers/specs/2026-09-07-ai-schedule-and-sync-design-zh.md)与[实施计划](superpowers/plans/2026-09-07-ai-schedule-and-sync.md)。

完成这条主线后再进入新用户开通、费用方案和 TestFlight。下方历史 Phase 保留作交付记录，旧确认策略以当前主线规格为准。

## 新增待办（2026-09-07）

- [x] 修复聊天等待反馈与失败/取消计时：已有 Trace 时仍显示“正在理解你的意思…”/“正在整理日程…”；结束后移除，取消前不写入日程。步骤记录实际耗时。31 项 App 和 3 项 UI 回归、真实 GLM 联合复测通过，见[验收记录](qa/2026-09-07-ai-schedule-sync-progress.md)。
- [ ] 优化完整聊天回合时延：较早四个合成回合约 4.9 / 13.0 / 14.3 / 4.0 秒；最新约 4.5 / 6.2 / 5.8 / 5.2 秒。最新路由约 1.7–2.8 秒、日程整理约 2.4–3.6 秒，当前每次修改包含两次模型调用。先扩大分阶段样本再决定优化；UI 修复不等于模型加速，单独 API 耗时不代表用户完整等待。

- [ ] 语音细节保真验收：覆盖数字、时间单位、否定表达和口头纠正，用自然口述及明确距离/噪声条件复测。当前真机链路已通过，但固定句出现关键遗漏，见[真机测试记录](qa/2026-09-07-physical-speech.md)。

- [ ] 正式向其他用户开放前，设计语音与聊天服务的开通和配置体验：明确由用户自带 Key 还是应用提供服务，处理费用提示、额度与用量、凭据保护和更换流程。验收应覆盖新用户从首次打开到完成一次语音输入并收到 AI 回复的完整流程，尽量只要求填写必要信息。
  - 当前仅在开发者真机测试阶段使用已授权的个人百炼 Key；这不作为其他用户的默认方案，也不将该 Key 嵌入安装包。

## 当前阶段

当前目标是完成 **M1：本地闭环原型**。四个页面的大局职责已经确定，接下来
按“设计确认 -> 实现 -> 自动检查 -> 模拟器截图/人工验收”的顺序逐阶段交付。

## 执行原则

- 每个阶段先给出可判断的设计或需求结果，再进入实现。
- 自动检查通过不等于视觉验收通过，两者分别记录。
- 明确请求的日程修改默认立即执行并可撤销；修改前确认可在设置中开启。讨论与 Dreaming 保持草稿。
- 优先完成真实闭环，不为未来能力提前增加界面负担。
- 每个阶段只提交自身文件，不混入工作区已有的无关改动。

## Phase 0：计划页检查点

目标：把已经确认的 A 版计划页固化为可追踪基线。

- 空白、AI 追问、草稿待确认三个状态。
- 聊天式低门槛输入，不强制用户先填写结构化表单。
- 草稿可保存；确认后通过 `V2PlanEngine` 原子写入正式计划。
- 核心检查、iOS 构建、紧凑尺寸截图与产品规格同步。
- 创建只包含本阶段文件的 scoped commit。

## Phase 1：回想页设计与接线

目标：把已有证据能力变成安静、可日常使用的反思页面。

- 先用 Creative Production 形成一版可判断的主界面设计。
- 默认只呈现写作面和一句 placeholder，不铺开摘要、比例或证据详情。
- 证据引用采用轻量入口；需要时再展开选择和定位。
- 接入 `V2RecallEngine` 的日反思、执行证据和保守偏差查询。
- 以空白、写作中、引用证据三个状态完成模拟器验收。

状态：已完成。证据窗默认关闭，按需覆盖在编辑器右上方；文字和引用关系按天
持久化，未保存草稿在日期切换时保留。

## Phase 2：端到端本地闭环

目标：用一个真实场景验证四个页面不是四个孤立原型。

验收场景采用“这周我想跑 10 公里”：

1. 用户在 `计划` 中自然表达意图。
2. 系统追问必要信息并生成结构化草稿。
3. 用户确认后，安排出现在相应日期和 `任务 / 时间`。
4. 当日任务进入 `今天`，产生真实开始、暂停、结束与用时记录。
5. `回想` 能引用这些执行证据完成当天反思。

该阶段结束时，M1 完成。

状态：自动闭环检查已通过。跨页面人工操作和真机验证保留到 Phase 7 的独立
QA，不把自动检查提前表述为最终体验验收。

## Phase 3：真实 AI 计划能力

目标：把确定性原型规划器替换为可测试、可降级的模型客户端。

- 固定输入、输出与系统场景，输出继续经过本地域模型校验。
- 并行判断是否需要拆解、规划时间、关联目标或作为孤立任务。
- 模型失败时保留用户输入和草稿，不产生静默写入。
- 真实模型与本地确定性测试替身分离，保证核心检查可重复。

状态：代码与契约已完成。App 通过 `V2PlanningClient` 调用规划器，模型返回的
日期、时间、任务引用会先在 Core 中验证。计划页的“AI 服务”可启用
OpenAI-compatible `/v1/chat/completions` 供应商，默认预填硅基流动地址和建议
模型；API Key 只存入本机 Keychain，服务地址必须使用 HTTPS。未启用在线规划时
明确使用“本地基础规划”，远程错误直接呈现，不静默替换结果。

开发环境仍可通过以下变量直接测试 OpenAI Responses API；该路径使用严格 JSON
Schema 且关闭服务端存储：

开发联调配置：

- `TOUGH_TRIAL_AI_API_KEY`
- `TOUGH_TRIAL_AI_MODEL`，未指定时使用 `gpt-5-mini`
- `TOUGH_TRIAL_AI_ENDPOINT`，未指定时使用 OpenAI Responses API

真实供应商联调仍需要用户在 App 内填写自己的 API 凭据，不纳入无凭据的本地
通过结论。发送规划请求时，用户输入、相关任务和已启用记忆会离开设备并交给所选
供应商处理。

## Phase 4：用户记忆与上下文

目标：让计划建议逐步理解用户，而不是要求用户反复填写约束。

- 只保存可解释、可查看、可更正的习惯与偏好。
- 区分明确事实、模型推断和临时上下文。
- 记忆只影响建议，不绕过用户确认。

状态：已完成本地记忆闭环。记忆独立保存在 `v2-memory.json`，计划请求只读取
当前有效版本；用户可从计划页菜单查看、新增、纠正或彻底忘记。纠正会保留版本
来源，忘记会物理删除整条版本链，7 天临时上下文到期后不再进入规划请求。当前
没有自动提取或自动写入记忆，模型推断仍需后续由用户明确确认。

该阶段结束时，M2：可日常使用的 MVP 完成。

## Phase 5：Dreaming

目标：在已有真实使用证据后提供主动但克制的建议。

- 至少积累 7 天真实执行与计划数据后再启动。
- 只包含两类建议：空闲时间安排、长期目标继续拆解。
- Dreaming 不进入 `今天`，也不自动写入持久数据。

状态：已完成受门槛保护的本地候选层。滚动 28 天内必须同时满足：至少 7 个
有证据的不同日期、至少 3 个执行日、至少 3 个计划日、首尾跨度至少 6 天。
空闲排期只读取用户明确保存的星期与时间窗口，日历空白不会被推断为空闲；
目标拆解只针对未完成且没有子节点的长期目标。候选仅在 `计划` 空白页出现，
点开后仍是普通待确认草稿，不写入 `今天` 或持久快照。

## Phase 6：iOS 原生能力

- 通知、Live Activity、语音输入与必要的系统快捷入口。
- Apple Pencil / 类 GoodNotes 手写作为 `回想` 的后续目标。
- 每项能力独立验证价值，避免一次性扩大 MVP。

状态：原生能力的第一版代码闭环已完成。通知权限只在用户从 `今天` 菜单主动
开启时请求，之后新确认的带时间计划会在已授权前提下自动登记提醒；当前聚焦的
执行会同步到 Live Activity，并可通过 `toughtrial://today` 回到 `今天`。
`计划` 输入框支持语音转写，`今天` 加号支持点按文字、长按语音与上拖锁定，
识别结果都先进入可编辑文本，提交后才写入任务。`回想` 增加了独立的
PencilKit 手写稿入口，手写文件按日期旁路保存，不改变正文或证据关系。

`swift run ToughTrialV2Checks` 与包含 Live Activity 扩展的通用 iOS Simulator
构建已通过。系统权限弹窗、锁屏展示、真实麦克风和 Apple Pencil 手感仍属于
Phase 7 的真机验收，不提前标记为通过。

## Phase 7：独立 QA 与 Beta

- 核心行为回归、JSON 持久化恢复和异常路径检查。
- 主要 iPhone 尺寸的视觉回归与可访问性检查。
- 真机连续使用、性能和数据安全检查。
- TestFlight 交付与问题分级。

状态：本地 Beta 候选已达到 Conditional Pass。核心检查、Swift 构建、通用
iOS Simulator 构建、无签名 `iphoneos` Release 构建均通过；4 个 UI 测试已在
iPhone 17 Pro 与 iPhone 17e 分别通过，四个主页面的双尺寸截图已人工检查。
测试过程中发现并修复了蓝色加号普通点按被长按手势吞掉、孤立任务结构重复绘制
两项真实回归。完整证据见
[`docs/qa/2026-07-24-v1-beta-readiness.md`](qa/2026-07-24-v1-beta-readiness.md)。

TestFlight 仍为 Blocked：本机 Xcode 没有当前 Team 的有效账号会话，主 App 与
Live Activity 扩展均缺 provisioning profile。真实 AI 凭据联调，以及通知、
Live Activity、麦克风、Apple Pencil 和连续使用仍需真机门禁。因此 M3 尚未
标记为最终完成，不把本地构建提前表述为已交付 Beta。

该阶段结束时，M3：V1 Beta 完成。

### 助手语音识别切换（2026-09-07）

- [x] 设置切换苹果原生 / 阿里云 FunASR，记住选择，保留云端密钥；助手右上角菜单可直接打开语音设置。
- [x] 苹果 iOS 26+ SpeechAnalyzer 端侧中文路径：可用性检查、模型准备、实时稿、完成后定稿提交、取消和错误保护。
- [x] 真机确认苹果中文模型可用性与两种方式的麦克风到 AI 链路，完成同一条约 10 秒录音直接输入对比；见[测试报告](qa/2026-09-07-speech-comparison.md)。
- [ ] 使用自然口述、长段录音、口音与嘈杂环境扩大语义和延迟样本，再决定默认识别方式。当前单条样本关键细节保留，同音字仍有错误。


## 2026-09-10：理账计划、附件与模块框架

- 已实现：订阅/账单/信用卡/房租/借款还款计划，月/年周期、提醒配置、话术复制、应用链接。
- 已实现：确认已支付生成标准流水、撤销、预算按币种与分类统计实际支出。
- 已实现：订阅与随手记共享原件附件，图片/PDF 缩略图、其他格式图标、预览与分享。
- 已实现：内置模块注册/启停/依赖，声明式 JSON 表单插件加载；示例见 `docs/plugins/`。
- 后续：真机到期通知及各应用 scheme 实测；文件和财务数据的多设备同步；更完整的插件扩展点与第三方开发工具。
- 当前边界：本地文件附件不是自动上传到云端；声明式插件不兼容 Obsidian JavaScript 插件。


## 框架实施（2026-09-10，第一批）

[详细设计](superpowers/specs/2026-09-10-plugin-runtime-and-contracts-design-zh.md) · [实施与验收](qa/2026-09-10-plugin-runtime.md)

- 已实现：业务模块与页面显隐分离；规范 ID、依赖状态、未知模块拒绝；旧设置迁移及隐式暂停保留。
- 已实现：Engine 事务边界检查能力，混合 Capture 逐目标检查；Finance 6 类 typed commands 与支付回执投影。
- 已实现：AI/语音/同步 generation ticket；文件协调器与系统导出 lease；财务提醒停用保护。隐藏页面仍可经快捷入口使用已启用功能。
- 已于完整批次实现：全部静态业务提交的命令登记、模块级 context/查询、作业清理、持久 outbox、schema 2 迁移备份。
- 已于完整批次实现：AI 动态工具目录、实际领域执行及原子回执、分类/支付确认、重试/撤销/聊天恢复；固定 schema 内的 typed 扩展字段；声明式包 v2。
- 完整批次验收与真机信息：[2026-09-10 完整实施](qa/2026-09-10-complete-module-runtime.md)。第三方开发工具、脚本执行器和插件市场仍属后续。
- 独立后续：财务/附件/新领域同步协议与 migration 协調、真机到期通知及外部应用跳转验收。

## 助手上下文与真实会话修复（2026-09-10）

- 已实现：本地 memory Markdown 投影、追加会话日志、可检索索引、有来源 Compact；AI 动态工具读记忆/原文/Trace 并压缩上下文。
- 已修复：引用未传模型、hello 继承历史写入、今天新增未进入当天计划；统一上下文清单进入 Trace。
- 助手新增“记忆与上下文”入口。验证与安装状态见[验收记录](qa/2026-09-10-assistant-context-memory.md)。
- 后续：真实口述任务拆分质量评估、更多自然表达的意图覆盖；记忆/归档的独立同步协议。
