# 路线 1–5 收尾验收核对

2026-09-07。依据 [批准的规格](../superpowers/specs/2026-09-07-ai-schedule-and-sync-design-zh.md) 和 [实施计划](../superpowers/plans/2026-09-07-ai-schedule-and-sync.md) 逐项核对。

结论：路线 1–5 的实现、本机/模拟器及真实云端验证已有证据；**整体完成仍未成立**。本次核对时 iPhone 13 Pro unavailable，GitHub 主线尚需实际手机上的模型配置及端到端同步验收。外部文件授权和 iCloud 只属于可选文件绑定路径，不是 GitHub 同步的前置条件。模型时延优化、新用户开通、费用方案和 TestFlight 属于后续工作，不用它们替代或扩大本轮验收。

## 要求与证据

“通过”仅表示该行注明的范围；不表示真机体验或所有自然语言表达均已验证。

| 要求 | 当前证据与范围 | 判定 |
| --- | --- | --- |
| 白名单新增、修改、排期、延期、取消排期、完成、恢复、归档与父子关系 | `ScheduleCommandChecks`、`ScheduleClientChecks`；混合命令和逐操作解码，检查由主入口实际执行 | Core 通过 |
| 整轮原子保存、失败不部分写入、回执与领域同次保存 | `checkScheduleCommandAtomicRollback`、`checkScheduleCommandAppliesAndPersistsReceipt`，含磁盘核对 | Core 通过 |
| requestID 幂等、旧快照兼容、过期提案拒绝 | 同一请求重载不重复；`checkScheduleCommandLoadsLegacySnapshotWithoutReceipts` 与 baseline/DST 检查 | Core 通过 |
| 撤销保留无关更改、后续关联更改和执行事实受保护 | Undo/ArchiveUndo/Reschedule/UndoRejectsLaterContextChild 检查 | Core 通过 |
| 实际高亮、可选严格确认、取消不写入 | `V2AssistantScheduleTests`、`V2ScheduleConflictTests`、`ToughTrialScheduleTests`；真实 GLM App 联合场景核对真实回执和高亮 ID | App 与模拟器通过 |
| 改口、具体限制、时区/相对日期、无日期任务树、同名歧义 | `V2ScheduleLiveTests` 的真实 GLM 样本；App 联合测试还覆盖省略名称的连续改期、原任务/排期 ID 不变 | 有限真模型样本通过 |
| 讨论不写入、非法结果拒绝、取消/切会话不接受迟到写入 | 真模型只读/歧义样本；客户端错误检查；助手取消与会话切换测试 | 对应边界通过 |
| 恢复后可撤销、严格提案持久化、重复确认仅写一次 | `V2AssistantLiveScheduleTests` 经真实路由和日程客户端，从 JSON 重建两个 Store，核对任务及回执 | App 状态重建通过；不是物理重启 |
| 等待提示、停止、失败/取消耗时 | 31 项 App + 3 项 UI 回归；受控时钟验证 9 秒取消/7 秒失败；截图人工查看 | 模拟器通过 |
| Trace 录音、转写修正、输入、AI、手动编辑、日程、文件、同步/冲突接入 | 核对 `V2AssistantView`、`V2AssistantStore`、`V2AssistantTurnLoop`、`V2AppStore`、文件/同步/冲突入口。语音按会话关联，助手/文件/同步按操作 ID 关联；手动编辑是独立事件 | 接线已完成 |
| Trace 上限、过期、关闭、重开、损坏保护、查看/导出/清空 | `UsageTraceChecks`；本轮 `ToughTrialUsageTraceTests` 与文件导出关联 UI 测试；真实 GLM 联合测试检查导出不含凭据或合成备注 | Core/App/UI 通过 |
| Markdown 稳定身份、手改字段、新行补 ID、未知正文保留 | RoundTrip/HumanFields/StableIDs/TextEncoding 检查；协议标记作为普通备注文字也能往返 | Core 通过 |
| 坏文件、重复 ID、截断、错误引用保全，缺行不隐式删除 | BrokenDocuments/MissingRows/ExecutionIntegrity 检查；App 实际坏文件与失败写入测试 | Core/App 通过 |
| 三方不同字段自动合并、同字段保留冲突、执行事实不可篡改 | Merge 系列、ConflictCannotRewriteExecutionFacts、真实 GitHub 双客户端场景 | Core/真实 GitHub 通过 |
| 本地绑定、手改读取、App 写回、重新导入、恢复并再次启动 | 本轮 `V2ScheduleFileTests` 2 项与 `ToughTrialScheduleFileTests` 3 项；完整 UI 场景实际终止并重启模拟器 App | 模拟器本地文件通过 |
| GitHub 指定位置、SHA 条件写入、401/409/422/离线/并发/重试 | GitHubSchedule/Sync 检查；配置 UI 无效位置拒绝且不连接；真实 GitHub stale SHA 被拒绝 | Core/UI/真实服务通过 |
| 基线与待上传持久化、在途本地编辑保留、旧目的地结果拒绝、前台自动同步边界 | `ScheduleSyncChecks` 与 App 自动同步测试；真实双客户端重载后联网重试 | 对应场景通过；前台同步不等于后台常驻 |
| 本机发起真实 AI 冲突处理、校验版本/字段、恢复与可选确认 | `ScheduleConflictChecks`、App 冲突测试；GLM 将双方备注合并并撤销，保持示例/配图/时间上限 | Core/App/真实 GLM 通过 |
| 远端处理稳定请求、重复/并发安全、真实自动回写 | RemoteRequest/RemoteRunner 检查；GLM Actions `34087032219` 实际处理步骤 success，合成限制仍在远端文件 | 已部署并真实执行 |
| 当前 GLM 部署重复触发不产生重复写入 | 本轮 Actions `34096096758`：Processed 0、pending 0、retries 0，日程 SHA 前后完全相同 | 本轮真实云端通过 |
| 两个独立持久客户端真实合并、AI 冲突、恢复、离线后重试、不重复提交 | `V2GitHubLiveTests.testTwoPersistedClientsConflictAIRecoveryAndRetry` 14.239 秒通过；本轮重新核对其远端文件 SHA 保持验收值 | 两个 Mac 客户端通过；手机尚待验收 |
| 实际手机模型配置、文件提供者/iCloud/bookmark 重启授权、手机与 Mac 同步 | 用户暂无真机；本轮设备列表仍 unavailable。模拟器与云端结果不能证明此项 | 等待设备 |

## 本轮可复核结果

- `swift run ToughTrialV2Checks`、`swift run FocusTimelineCoreChecks`、`swift build` 全部 exit 0。日志分别为 `/private/tmp/tough-trial-route-audit-core.log`、`tough-trial-route-audit-compat.log`、`tough-trial-route-audit-build.log`。
- 本轮 2 项 App 文件测试与 5 项 UI 测试全部通过，0 失败、0 跳过，xcodebuild exit 0。UI 包括同步配置拒绝、系统文件选择器、文件 Trace 关联、完整文件往返/重启/恢复，以及 Trace 关闭/导出/清空。日志 `/private/tmp/tough-trial-route-audit-ios.log`；结果包 `/private/tmp/tough-trial-apple-sim/Logs/Test/Test-ToughTrial-2026.09.07_15-31-51-+0800.xcresult`。
- 此前最终真实 App/GLM 联合结果 `/private/tmp/tough-trial-progress-live-final.log` 仍与当前实现一致：1 项、0 失败、0 跳过；失败轮次及日期精度断言修正保留在[过程记录](2026-09-07-ai-schedule-sync-progress.md)，不将失败轮次记为通过。
- 45 个运行器/Core 文件的 SHA256 与根工作区、部署副本逐项一致，并将各文件 Git blob SHA 与 GitHub 当前树逐项比对一致。证据 `/private/tmp/tough-trial-route-audit-remote.json`。
- [GLM 首次回写](https://github.com/liuxiaoyusky/tough-trial-sync/actions/runs/34087032219) 与 [当前部署重复触发](https://github.com/liuxiaoyusky/tough-trial-sync/actions/runs/34096096758) 均完成。重复运行确实进入处理步骤，输出 `Processed: 0; pending: 0; conflict retries: 0.`；日志 `/private/tmp/tough-trial-route-audit-actions.log`。`schedule.md` 前后 SHA 均为 `4f36cb72860f6caadeed079fa23eab197daa8f8b`。
- 双客户端合成文件 `acceptance/two-clients-9183b1b0-7f2a-49f4-8306-92ff480bc2ce.md` 当前 SHA 为 `4fa29d0bd0ace7fa469354b25a00b2a5fca69789`，与原真实验收一致。该测试位于 `live-routing-conflict.log`，其中另一个首次聊天路由测试失败，不能把该日志整体称为通过。
- 设备核对 `/private/tmp/tough-trial-route-audit-devices.log`：iPhone 13 Pro unavailable。没有安装新构建、修改手机活动模型或模拟通过文件授权。

## 设备恢复后直接完成的验收

1. 安装当前构建，配置已授权 GLM 服务；在手机聊天新增带改口/限制的合成日程，核对实际列表、高亮、严格开关和撤销。
2. 可选外部文件路径单独验收：从手机系统文件选择器绑定测试 Markdown，经实际文件提供者修改后重新读取；终止并重开 App 后再次读写，确认授权与 bookmark 可恢复。仅使用 iCloud 文件时验证 iCloud；不阻塞 GitHub 主线。
3. 手机与 Mac 客户端对同一个私有测试文件分别修改；完成正常同步、同字段冲突处理、撤销和离线后重试，并核对远端 SHA 与实际任务。
4. 实际口述后点完成，核对转写到日程的链路与 Trace；保留失败及细节遗漏样本。自然口述质量不能用固定录音样本的通过替代。

上述设备门禁已经连续保留多个推进轮次。当前可独立推进的收尾验证已完成；目标应等待设备恢复后继续，不能标记为整体完成。
