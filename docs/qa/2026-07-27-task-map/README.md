# T201 任务结构图自动验收

- 日期：2026-07-27
- 状态：自动化门禁通过，等待人工手势与视觉验收

## 已验证

- 展开 `定位` 后继续展开 `选题库`，两个父节点可同时保持展开。
- 收起 `定位` 不会连带收起 `选题库`。
- 布局核心为每棵可见子树分配独立纵向区间。
- 分支总高度超过固定视口时，画布高度会增长并可纵向滚动。
- 画布支持横向滚动、缩放和回到默认比例。

## 自动化证据

- `swift run ToughTrialV2Checks`：Pass
- generic iOS 无签名 build：Pass
- `testTaskMapCollapseAndZoomControls`：Pass
- 截图：`task-map-multiple-branches-expanded.png`

## 未自动替代

人工仍需在模拟器或真机确认：

1. 上下左右拖动是否自然；
2. 双指缩放与拖动是否冲突；
3. 默认聚焦是否符合第一眼阅读；
4. 不同缩放比例下的文字、连接线和按钮是否舒适。
