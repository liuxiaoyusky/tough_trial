# MiniMax 海外配置故障 · 2026-09-11

版本：1.0 (6)。用户使用海外 Coding Plan；诊断不包含 Key 正文，也不重发私人会话。

## 证据与结论

- 设备 MiniMax 配置实际为 `https://api.minimax.cn/v1`，模型 `MiniMax-M2.7-highspeed`。用户会话 Trace 返回 unauthorized，失败步骤约 0.45 秒。
- 原设置页在保存时强制使用国内预设地址，且不能选择地区。这是已确认的配置缺陷。
- 用同一设备 Key 对官方海外 `https://api.minimax.io/v1/chat/completions` 发出独立合成连接测试，仍返回 HTTP 401 / 1004。不能声称仅改地址已解决实际连接。
- 设备内只检查 Key 格式：含内部空白，不含遮罩，不以常见订阅或 API Key 前缀开头。格式检查未输出凭据。需要用户确认粘贴内容，不能推断套餐无额度或模型不支持。

## 修复

- 新 MiniMax 配置默认海外，已保存的国内或自定义地址保持原样。
- 服务与地区使用原生选择列表；保存不再覆盖地区。表单直接展开，避免半屏叠加菜单的交互问题。
- MiniMax Key 若包含内部空白/换行，在配置阶段提示只粘贴完整 Key。首尾空白仍安全去除；不从任意文本自动猜取密钥。
- 用户已明确授权海外站，因此单独修正其手机的地区配置，同时保留原 Key。修正地区不表示鉴权验证通过。

## 验证

- 默认海外端点测试先失败、修复后通过。
- 内部空白 Key 校验测试先失败两项断言、修复后通过。
- 6 项配置回归通过。
- 设备地区保存测试通过，确认海外地址生效且 Key 保留；1.0 (6) 已安装，16:21:08 启动成功。
- 原生地区选择 UI 回归通过，验证国内/海外切换后的实际值。测试按原生列表返回流程执行。
- 真实海外连接未通过，原因仍为鉴权；没有反复调用无效凭据。

参考：[MiniMax 官方 OpenAI 接口](https://platform.minimax.io/docs/api-reference/text-openai-api)。原始诊断保存在本机临时目录 `/private/tmp/tough-minimax-*`，不随项目分发。

## 后续状态

用户重新配置后，鉴权已通过。1.0 (7) 增加测试连接、修复工具回复兼容并完成真实海外测试，详见 [连接测试验收](2026-09-11-ai-connection-test.md)。以上 401 为当时证据，不是当前连接状态。
