# 今日执行队列

用户确认，2026-09-20。

- 主卡片表示当前关注的运行任务，执行中列表只显示其他运行会话，不重复显示主卡片。队列保留现有会话顺序。
- 暂停保存并结束本段，移回今日规划。继续开启新的执行段，累计不清零。旧暂停会话同样归入今日规划，可恢复。
- 完成保存执行时间并完成任务，今日规划中保留完成态，可恢复未完成。
- 暂停/完成后原队首自动成为主卡片，从执行列表移除；计时连续，不新建段。无队首则空闲。
- 今日规划含未运行、暂停和完成的当日任务；所有运行任务从该分区排除。
- 主卡片显示本段时长、同任务今日有效时长和跨天累计时长，不显示开始时间。所有队列/规划条目显示累计与今日。暂停间隔不计入。
- 无关联任务的自由 Zen 不伪造任务完成：结束时间段保留执行记录。
- 原生页面验收后以真实模拟器截图更新评审素材，注明截图状态和示例数据，不以重绘代表当前实现。
