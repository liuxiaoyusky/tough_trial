# Tough Trial 项目进度

更新日期：2026-07-23

## 当前结论

产品的大局设计已经稳定，当前工作重点不是继续增加页面概念，而是把四个
页面接成一个可真实使用、可验证的本地闭环。

| 页面 | 产品职责 | 当前状态 | 下一缺口 |
| --- | --- | --- | --- |
| `今天` | 只处理今天的执行与真实用时 | V2 执行界面已保留计划项来源并记录真实用时 | 真机连续使用验收 |
| `任务` | 看目标、拆解、历史、未来和终点 | 已有结构、时间、鱼骨视角；结构图支持拖动、缩放与完成信号 | 继续以真实任务数据验证可读性 |
| `计划` | 聊天优先的 AI 计划工作区 | A 版三状态支持只排期、只拆解及原子 mixed 草稿 | 接入真实模型与用户记忆 |
| `回想` | 基于真实执行证据进行反思 | V2 日期轨道、写作面、按需证据窗已连接本地持久化 | 与计划、执行完成端到端验收 |

## 已完成的基础能力

- 任务、执行片段、计划草稿和回想证据都有独立的 V2 领域模型与本地 JSON
  持久化入口。
- `今天` 已遵守“只专注今天”的边界，不展示长期目标分类或 Dreaming 建议。
- `计划` 已形成空白、AI 追问、草稿待确认三个核心状态。
- 计划草稿只有在用户确认后才写入正式计划；`存草稿` 与 `加入计划` 使用既有
  `V2PlanEngine`。
- “这周跑 10 公里”可以确认成一棵父子任务树和三条绑定叶子任务的计划项。
- 计划项 ID 会贯穿今天执行与回想证据，不再依赖标题猜测计划和执行是否匹配。
- 自动检查已经覆盖“聊天草稿 -> 用户确认 -> 今天执行 -> 回想引用 -> 重启恢复”。
- 计划页已经在 iPhone 17 Pro 与紧凑尺寸上完成截图检查。
- 回想页已经使用真实执行、偏差和过去事件生成引用候选，正文与引用关系可以按天
  保存和恢复。
- 产品规范、视觉语义角色和工程入口已经分别收敛到 `docs/spec.md`、
  `docs/design-system.md` 与 `CLAUDE.md`。

## 仍需诚实标注的边界

- 当前计划对话由确定性原型逻辑生成，还不是真实大模型。
- 用户习惯、生活节奏和历史偏好尚未形成可控的记忆层。
- Dreaming 尚未实现；在获得至少 7 天真实使用数据前不应提前开发。
- 尚未完成独立 QA、真机长期使用和 TestFlight 交付。

## 当前检查点

Phase 1 已完成回想页真实证据接线和模拟器视觉检查。Phase 2 已完成本地领域
闭环与自动验收；最终跨页面人工操作验收统一放在独立 QA 阶段执行。工作区内旧
V1、历史脑暴稿和其他协作材料继续保持原样。

2026-07-23 验证结果：

- `swift run ToughTrialV2Checks`：通过。
- `xcodebuild` generic iOS Simulator build：通过。
- 计划页四张本地截图证据保留在 `outputs/simulator/`，不纳入本次源码提交。
